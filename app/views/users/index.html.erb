<h1>ユーザー一覧</h1>

<% @users.each do |e| %>
  ユーザーID: <%= e.uid %>, パスワード: <%= e.pass %> 
  <%= link_to '削除', user_path(e), data: { turbo_method: :delete } %>
  <br>
<% end %>

<%= link_to '新規登録', new_user_path %>
<%= link_to 'ログイン', root_path %>