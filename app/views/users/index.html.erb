<h1>ユーザー一覧</h1>
<ul>
  <% @users.each do |u| %>
    <li>
      <%= u.uid %> 
      <%= link_to '削除', user_path(u), method: :delete %>
    </li>
  <% end %>
</ul>


<%= link_to '新規登録', new_user_path %>